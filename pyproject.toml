[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "abstractagent"
version = "0.3.11"
description = "Agent implementations using AbstractRuntime and AbstractCore"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "abstractcore[tools]>=2.13.32",
    "abstractruntime>=0.4.27",
]

[project.optional-dependencies]
apple = [
    "abstractcore[all-apple]>=2.13.32",
    "abstractruntime[apple]>=0.4.27",
]
gpu = [
    "abstractcore[all-gpu]>=2.13.32",
    "abstractruntime[gpu]>=0.4.27",
]
dev = [
    "pytest>=7.0",
]
test = [
    "pytest>=7.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
react-agent = "abstractagent.repl:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
markers = [
    "basic: Level A (contract/unit) tests",
    "integration: Level B (integration/local) tests",
    "e2e: Level C (end-to-end/real infra) tests (opt-in)",
]
